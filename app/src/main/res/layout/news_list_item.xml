<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/button"
        android:layout_width="49dp"
        android:layout_height="37dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="21dp"
        android:background="@drawable/player_ic_red" />

    <RelativeLayout
        android:id="@+id/recipe_list_text_layout"
        android:layout_width="281dp"
        android:layout_height="90dp"
        android:layout_centerVertical="true"
        android:layout_marginEnd="13dp"
        android:layout_toEndOf="@id/news_list_image">

        <TextView
            android:id="@+id/news_list_description"
            android:layout_width="261dp"
            android:layout_height="59dp"
            android:layout_below="@+id/news_list_titulo"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="3"
            android:textSize="16sp"
            tools:text="Description" />

        <TextView
            android:id="@+id/news_list_titulo"
            android:layout_width="258dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:textSize="18sp"
            tools:text="Title" />

    </RelativeLayout>

    <ImageView
        android:id="@+id/news_list_image"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="6dp"
        android:layout_marginStart="4dp"
        android:layout_marginTop="6dp"
        android:contentDescription="@string/thumbnail"
        android:scaleType="centerCrop"
        tools:src="@mipmap/ic_launcher" />

</RelativeLayout>